/**
 * settings/orders
 *
 * Orders table settings UI.
 *
 * Currently lives inside settings/SettingsModal.jsx:
 *   - OrderFieldTable   — field visibility, labels, palette, highlight
 *   - PricingTab        — price rules and color mapping
 *   - ViewTab           — search fields, default sort, visibility
 *   - DatesTab          — date display and search format
 *
 * Future extractions planned into this folder.
 */
