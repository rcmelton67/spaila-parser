/**
 * settings/email
 *
 * Email template settings UI.
 *
 * Currently, the EmailsTab component lives inside:
 *   settings/SettingsModal.jsx
 *
 * Future extraction planned:
 *   - EmailsTab.jsx   — template list, condition editor, attachment editor
 */
