/**
 * shared/components
 *
 * Reusable UI components shared across features and settings.
 *
 * Future candidates:
 *   - EyeIcon.jsx         — show/hide toggle icon
 *   - PaletteIcon.jsx     — palette/color icon
 *   - HighlightPicker.jsx — highlight toggle + color swatch picker
 *   - SortableHeader.jsx  — draggable table header (currently in OrdersPage)
 */
