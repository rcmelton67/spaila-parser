# Spaila Shared Contracts

This folder holds browser-safe contracts and utilities shared by desktop and web surfaces.

- `models/` defines stable domain constants for orders, conversations, archive search, account, subscription, and feature capabilities.
- `api/` defines endpoint names and a small fetch client for the operational backend.

Parser execution, helper control, backup/restore, and local filesystem authority remain desktop-only.
